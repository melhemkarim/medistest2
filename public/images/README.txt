Put your photo here, named exactly:
photo-1.jpg

This one photo is used as the background across every screen of the
invitation (all 4 panels) plus the fallback and "not found" pages, for a
consistent look throughout the site.

Any size/aspect ratio works — it's cropped automatically. If it's missing,
the site falls back to a generated brand-blue gradient instead of breaking.
